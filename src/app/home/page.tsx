import HomePage from "@/components/home/HomePage";

/** App home (logged-in layout), reachable without replacing the marketing landing at `/`. */
export default function AppHomePage() {
  return <HomePage />;
}
