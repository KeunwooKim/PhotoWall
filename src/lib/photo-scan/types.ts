/** Quad corners in image pixel space: TL → TR → BR → BL */
export type QuadPoints = [
  { x: number; y: number },
  { x: number; y: number },
  { x: number; y: number },
  { x: number; y: number },
];

export type Point2 = { x: number; y: number };
