# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
.vercel

# production
/build

# misc
.DS_Store
*.pem
google_Client

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example

# Keep supabase SQL migrations in git for audit/repro.
# Secrets stay in .env* (already ignored).

# source archives for sticker packs (extracted copies live in public/)
/stickers-src/

# wallpaper masters (processed copies live in public/wallpapers/)
/.wallpaper-source/

# local runtime logs (cron / pm2 / deploy)
/logs/

# design references / obsolete mocks (not shipped)
/notion-ui/
/에셋유아이.png
/에셋유아이2.png
/미리캔버스_모바일화면.png

# typescript
*.tsbuildinfo
next-env.d.ts
